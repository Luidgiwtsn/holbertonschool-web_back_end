export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    /* Le corps de la promesse reste vide ici car 
       l'exercice demande simplement de retourner une instance */
  });
}
